{
  "name": "planac-erp",
  "version": "0.1.0",
  "private": true,
  "description": "ERP PLANAC - Sistema de Gestão Empresarial",
  "author": "PLANAC Distribuidora",
  "license": "UNLICENSED",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "db:migrate": "wrangler d1 migrations apply planac-db",
    "db:migrate:local": "wrangler d1 migrations apply planac-db --local",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "turbo": "^2.6.3",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.2.0"
}
